---
type: add
issue: 8391
jira: SMILE-12842
title: "A new `validateCode` method has been added to `IValidationSupport` which also takes the code system
  version to validate against. Existing implementations are unaffected, as it defaults to delegating to the
  existing method."
