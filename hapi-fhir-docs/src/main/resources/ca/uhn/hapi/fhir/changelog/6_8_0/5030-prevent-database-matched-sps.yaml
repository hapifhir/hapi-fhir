---
type: add
title: "A new configuration option, `JpaStorageSettings#setOnlyAllowInMemorySearchParams(boolean)` can now prevent the creation of custom Search Parameters 
that would need to be matched in the database. If enabled, only Search Parameters which can be evaluated IN-MEMORY are permitted to be created."

