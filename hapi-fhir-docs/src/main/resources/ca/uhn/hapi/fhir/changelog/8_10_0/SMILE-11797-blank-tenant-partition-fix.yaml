---
type: fix
jira: SMILE-11797
title: "Previously, a SystemRequestDetails with a blank tenant ID would bypass the partition interceptor chain
  in unnamed partition mode, causing a 'HAPI-2223: Partition IDs have not been set' error. Blank tenant IDs are
  now treated the same as null, allowing the interceptor chain to resolve the partition correctly."
