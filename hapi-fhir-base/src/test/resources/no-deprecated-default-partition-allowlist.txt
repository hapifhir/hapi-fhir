# Regression guard allowlist for GL-8692.
#
# Format: <relative-path-from-repo-root> <count>
# Lines beginning with '#' and blank lines are ignored.
#
# Each entry records a current call site of the deprecated
# RequestPartitionId.defaultPartition() / defaultPartition(LocalDate) overloads
# inside a src/main tree. As each stage of GL-8692 lands and a call site is
# rewritten to use IDefaultPartitionSettings.getDefaultRequestPartitionId(), the
# corresponding entry below must be decremented or removed in the same commit.
# After Stage R + Stage 8 this file is empty and the deprecated methods are deleted.
#
# Files in ca.uhn.fhir.jpa.migrate are restricted by CLAUDE.md and tracked
# in Stage R (owned by the user).
#
# The remaining non-migrate entries are calls inside @Deprecated enclosing
# methods/constructors that are themselves slated for removal; they are left in
# place until Stage 8 removes the deprecated defaultPartition() overloads.
hapi-fhir-jpaserver-model/src/main/java/ca/uhn/fhir/jpa/model/entity/PartitionablePartitionId.java 1
hapi-fhir-jpaserver-searchparam/src/main/java/ca/uhn/fhir/jpa/cache/IResourceChangeListenerRegistry.java 1
hapi-fhir-storage/src/main/java/ca/uhn/fhir/jpa/subscription/model/ResourceModifiedMessage.java 1
