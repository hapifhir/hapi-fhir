/**
 * Collects various repository implementations for HAPI FHIR.
 * <ul>
 *     <li>The {@link ca.uhn.fhir.repository.impl.memory.InMemoryFhirRepository} is a simple in-memory implementation suitable for testing.
 *     <li>The {@link ca.uhn.fhir.repository.impl.GenericClientRepository} uses GenericClient to access a REST repository over http/https.
 * </ul>
 */
package ca.uhn.fhir.repository;
